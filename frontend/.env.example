# Base da API. Em dev fica vazio/'/api' — o proxy do Vite encaminha para o
# backend local. As fotos são servidas pela própria API (/api/fotos/{key}),
# por isso não há aqui nenhum domínio de Blob Storage.
VITE_API_URL=/api
