// valores configuráveis do negócio (protótipo: props do bundler) — meta mensal
// e custo de envio, isolados aqui para não ficarem espalhados pelos componentes

export const PARAMETROS_MOCK = {
  /** Meta de vendas do mês (€). */
  metaMes: 1500,
  /** Envio CTT cobrado à cliente (€). */
  envioCobrado: 4.5,
  /** Custo real do envio CTT para o ateliê (€). */
  envioReal: 4.1,
};
